apiVersion: v2
name: nginx
description: A Helm chart for the Trello nginx proxy (Nginx with bundled frontend)
type: application
version: 0.1.0
appVersion: "1.0.12" # Matching your frontend image tag as a starting point